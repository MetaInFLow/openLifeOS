# Runtime Skills

这里记录运行期可调用或正在验证的 skills。

Runtime skill 负责执行具体任务、产生日志和 lesson，不等于 distilled meta skill。只有经过 IPO Reverse、owner alignment 和 privacy review 后，runtime 证据才可以升级为稳定方法论或可安装 skill。

推荐字段：`skill_id`、用途、触发条件、输入边界、输出位置、依赖资料源、当前状态、最近 lesson。
