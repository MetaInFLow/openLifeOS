# Runtime Sessions

这里记录 runtime 活动的公开安全索引。

每个 session 可以记录目标、时间、参与系统、输入边界、输出位置、关键 observation 和 lesson 指针。不要保存私密原文、secret、token、客户资料或未经授权的转写正文。

Session 的产出进入对应 work/artifacts/docs；观察和 lesson 指向 `runtime/runtime-lessons/`，再由 `evolution/ipo/` 复盘。
