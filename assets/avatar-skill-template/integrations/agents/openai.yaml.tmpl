interface:
  display_name: "{{DISPLAY_NAME}}"
  short_description: "{{DISPLAY_NAME}} 的数字分身与工作上下文入口"
  default_prompt: "使用 ${{SKILL_ID}} 读取 {{DISPLAY_NAME}} 的公开身份、PSP 和上下文，帮我判断下一步。"

policy:
  allow_implicit_invocation: true
