# Integrations 集成配置

这一层记录外部系统接入意图、权限范围和 token 存储策略。

规则：

- 不提交 token、app_secret、cookie、refresh token 或私钥。
- GitHub 权限配置写入 `integrations/github.yml`。
- Feishu/Lark 权限配置写入 `integrations/feishu.yml`。
- Hermes 增量同步配置写入 `integrations/hermes.yml`。
- 真实 secret 只放在本地环境变量、密码管理器或平台密钥系统。
