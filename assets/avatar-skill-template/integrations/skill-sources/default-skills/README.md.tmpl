# Default Skills

`integrations/skill-sources/default-skills/` 保存 openLifeOS bridge/兜底 Skill，不是完整 self-evolution Skill 仓库，也不是用户后续蒸馏出来的个人能力仓库。

当前说明：

- 完整 Wenxin Skill 安装在 `evolution/organ-systems/wenxin/`。
- 完整 PSP Skill 安装在 `evolution/organ-systems/psp/`。
- 完整 IPO Reverse Skill 安装在 `evolution/organ-systems/ipo-reverse/`。
- 完整 Taste Generator Skill 安装在 `evolution/organ-systems/taste-generator/`。
- 不要在 `integrations/skill-sources/default-skills/` 下放同名 Skill 副本。

用户自己的 runtime skill 或 distilled meta skill 默认进入 `capabilities/`。
