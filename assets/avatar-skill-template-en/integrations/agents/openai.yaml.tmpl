interface:
  display_name: "{{DISPLAY_NAME}}"
  short_description: "{{DISPLAY_NAME}}'s digital avatar and work context entrypoint"
  default_prompt: "Use ${{SKILL_ID}} to read {{DISPLAY_NAME}}'s public identity, PSP, and context, then help me decide the next step."

policy:
  allow_implicit_invocation: true
