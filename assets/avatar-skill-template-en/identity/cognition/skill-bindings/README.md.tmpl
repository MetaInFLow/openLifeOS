# Skill Bindings

Skill bindings connect procedures to facts and data sources without hard-coding those facts inside `SKILL.md`.

Use this layer when a skill needs:

- current user preference;
- project fact;
- connector config;
- memory wiki area;
- authority or visibility check;
- approved data source.

The skill should say what it needs. The binding says where the current truth comes from.
