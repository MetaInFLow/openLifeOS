# PSP Changelog

This log records active PSP/person-model changes. Raw private evidence is never stored here.

## {{CREATED_AT}}

- Activated `versions/PSP_REPORT.{{ARTIFACT_TIMESTAMP}}.xml` as the timestamped version behind `current/PSP_REPORT.xml`.
- Activated `versions/EVIDENCE_MATURITY.{{ARTIFACT_TIMESTAMP}}.xml` as the timestamped evidence maturity artifact behind `current/EVIDENCE_MATURITY.xml`.
- `SOUL.md` generation is paused; PSP XML carries the person model and runtime constraints.
- Current status is `evidence_sufficiency: insufficient` until representative behavior, judgment, and validation evidence is provided.
